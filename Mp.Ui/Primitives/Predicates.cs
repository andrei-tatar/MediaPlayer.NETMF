using System;

namespace Mp.Ui.Primitives
{
    public delegate bool StringValidator(string obj);
    public delegate void SimpleAction();
    public delegate bool SimpleActionParamString(string s);
}
